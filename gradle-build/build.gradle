subprojects {

    apply plugin: 'groovy'
    apply plugin: 'maven-publish'
    apply plugin: 'maven'
    apply plugin: 'eclipse'

    repositories {
        mavenCentral()
        jcenter()
    }

    dependencies {
        compile gradleApi()
        compile localGroovy()
    }

    publishing {
        publications {
            mavenGroovy(MavenPublication) {
                groupId 'de.monkeyworks.buildmonkey.build'
                version '0.1'

                from components.java
            }
        }
    }
}